# JFrog Demo — Python Catalog API
# Intentionally pinned to versions with known CVEs for Xray demo.
# Packages are below CVSS 8.0 so Curation allows download; Xray still flags them.
# DO NOT upgrade without updating the runbook.

# CVE-2023-25577 (CVSS 7.5) — Werkzeug multipart DoS via large boundaries
Werkzeug==2.2.2

# CVE-2023-32681 (CVSS 6.1) — requests leaks Proxy-Authorization on redirect
requests==2.27.1

# CVE-2023-43804 (CVSS 5.9) — urllib3 leaks Cookie header on redirect to different host
urllib3==1.26.6

# Web framework (no known CVE — clean dep for contrast)
flask==2.3.3
